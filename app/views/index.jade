doctype html
html
	head
		title=Welcome
	body
		p a(href="/auth/facebook" title="authenticate") Log in with facebook.
		- var url = req.user ? "/users/"+req.user.id : "/"
		p a(href=url) Profile
		p a(href="/logout") Log out
